export const metadata = {
  title: 'Dashboard — SmartPredict AI',
  description: 'Your AI-powered business insights dashboard',
};

export default function DashboardLayout({ children }) {
  return children;
}
